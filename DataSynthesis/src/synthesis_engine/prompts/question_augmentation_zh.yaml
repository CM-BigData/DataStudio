user: |
  请为下面的问题生成 {{ count }} 条问题增强变体。不要把问题拆解成推理子问题；保持答案意图不变，并保留原问题语言。增强应包括同义改写、搜索式问法、补充上下文的问法。仅返回 JSON 数组，格式为 [{"question": "...", "augmentation_type": "rewrite|search_query|context_variant"}]。
  原问题：{{ question }}
